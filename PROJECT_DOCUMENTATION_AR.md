# HearMeAI — مساعد التواصل الذكي لذوي الإعاقة السمعية والصوتية
## التوثيق الكامل للمشروع (عربي)

**الإصدار:** 1.0.0 | **التاريخ:** أبريل 2026

---

## فهرس المحتويات

1. [نظرة عامة على المشروع](#١-نظرة-عامة-على-المشروع)
2. [معمارية النظام](#٢-معمارية-النظام)
3. [التقنيات المستخدمة](#٣-التقنيات-المستخدمة)
4. [توثيق الـ Backend الخلفي](#٤-توثيق-الـ-backend-الخلفي)
5. [توثيق الـ Frontend الأمامي](#٥-توثيق-الـ-frontend-الأمامي)
6. [توثيق نماذج الذكاء الاصطناعي](#٦-توثيق-نماذج-الذكاء-الاصطناعي)
7. [توثيق قاعدة البيانات](#٧-توثيق-قاعدة-البيانات)
8. [توثيق الميزات](#٨-توثيق-الميزات)
9. [مسار تجربة المستخدم](#٩-مسار-تجربة-المستخدم)
10. [دليل التثبيت والتشغيل](#١٠-دليل-التثبيت-والتشغيل)
11. [الأمان والتحقق من المدخلات](#١١-الأمان-والتحقق-من-المدخلات)
12. [الاختبار والتصحيح](#١٢-الاختبار-والتصحيح)
13. [التحديات والحلول](#١٣-التحديات-والحلول)
14. [القيود الحالية](#١٤-القيود-الحالية)
15. [العمل المستقبلي](#١٥-العمل-المستقبلي)
16. [أسئلة الدكتور المتوقعة](#١٦-أسئلة-الدكتور-المتوقعة)
17. [نصوص العرض التقديمي](#١٧-نصوص-العرض-التقديمي)
18. [شرح الملفات المهمة](#١٨-شرح-الملفات-المهمة)
19. [الخلاصة النهائية](#١٩-الخلاصة-النهائية)

---

## ١. نظرة عامة على المشروع

### اسم المشروع
**HearMeAI** — مساعد التواصل الذكي للصم والبكم

### الفكرة الرئيسية
HearMeAI هو تطبيق ويب متطور يعمل في الوقت الفعلي ويهدف إلى كسر حواجز التواصل بين ذوي الإعاقة السمعية والصوتية من جهة، والأفراد العاديين من جهة أخرى. يجمع التطبيق ثلاث قنوات اتصال مدعومة بالذكاء الاصطناعي في منصة واحدة متكاملة:
- **تحويل الكلام إلى نص** (Speech-to-Text / STT) للمستخدمين الصم
- **تحويل النص إلى كلام** (Text-to-Speech / TTS) للمستخدمين البكم
- **مساعد لغوي ذكي** (AI Assistant) لتبسيط الرسائل وتوضيحها وترجمتها

### المشكلة التي يحلها المشروع
تشير إحصائيات منظمة الصحة العالمية (2023) إلى أن أكثر من **430 مليون شخص** حول العالم يعانون من ضعف السمع المُعيق. ويعاني ملايين آخرون من حالات تمنعهم من الكلام. تواجه هذه الفئة عقبات يومية حقيقية:

- لا يستطيع الأشخاص العاديون التواصل بسهولة مع الصم في الوقت الفعلي
- يعجز الأفراد البكم عن التعبير عن أنفسهم شفهياً أمام من يسمعون
- لغة الإشارة ليست معروفة على نطاق واسع، مما يضيّق دائرة التواصل
- التطبيقات الموجودة غالباً تفتقر إلى إمكانية الوصول، وتدعم الإنجليزية فقط، ولا تستفيد من الذكاء الاصطناعي

### الفئة المستهدفة

| فئة المستخدم | حالة الاستخدام |
|---|---|
| الأفراد الصم | قراءة ترجمة ما يقوله الآخرون في الوقت الفعلي |
| الأفراد البكم | كتابة النص ليُقرأ بصوت عالٍ عنهم |
| ضعاف السمع | الاستفادة من النصوص المكتوبة والترجمات المحسّنة |
| الأسر ومقدمو الرعاية | التواصل مع أفراد الأسرة الصم والبكم |
| العاملون في الرعاية الصحية | التواصل مع المرضى الصم والبكم |
| المعلمون والمدرّسون | دعم التعليم الشامل في الفصل الدراسي |

### الهدف الرئيسي
توفير منصة تواصل مجانية وقابلة للوصول ومدعومة بالذكاء الاصطناعي، تتيح التواصل الثنائي الفوري بين ذوي الإعاقة السمعية/الصوتية والآخرين، مع دعم كامل للغتين العربية والإنجليزية.

### لماذا يُعدّ هذا المشروع ذا قيمة؟
1. **شامل**: يعمل لصالح كل من المستخدمين الصم والبكم في آنٍ واحد
2. **ثنائي اللغة**: دعم كامل للعربية والإنجليزية مع تخطيط RTL للعربية
3. **ذكاء اصطناعي محسّن**: الرسائل لا تُنسخ فحسب، بل تُفهَم وتُحسَّن
4. **إمكانية الوصول**: متوافق مع معايير WCAG 2.1 AA/AAA
5. **الوقت الفعلي**: اتصال WebSocket فوري بدون تأخير ملحوظ
6. **قابل للنشر**: يدعم Docker وجاهز للنشر على Vercel وRailway

### ملخص تنفيذي
HearMeAI هو تطبيق ويب متكامل جاهز للإنتاج، مبني باستخدام Next.js 14 في الجانب الأمامي وFastAPI في الجانب الخلفي. يدمج نموذج OpenAI Whisper للتعرف على الكلام، وMicrosoft Edge TTS لتوليد الصوت، وOpenRouter (Claude 3 Haiku) للمعالجة اللغوية الذكية. يتميز النظام بالتواصل الفوري عبر WebSocket، ومصادقة المستخدمين، وإدارة الجلسات، ونظام مطابقة متعدد المستخدمين، وصورة رمزية ثلاثية الأبعاد للغة الإشارة داخل المتصفح، وميزات إمكانية وصول شاملة، مع دعم ثنائي اللغة ونشر سحابي على Vercel وRailway.

---

## ٢. معمارية النظام

### المعمارية الكاملة

```
┌─────────────────────────────────────────────────────────────────┐
│                      متصفح المستخدم (Browser)                   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │          Next.js 14 - الجانب الأمامي (React / TypeScript)  │  │
│  │  الصفحة الرئيسية ← المصادقة ← الردهة ← مساحة المحادثة    │  │
│  │                                                             │  │
│  │  [زر الميكروفون] → MediaRecorder → صوت Base64             │  │
│  │  [سجل المحادثة] ← رسائل WebSocket                         │  │
│  │  [مشغل الصوت] ← صوت TTS بصيغة Base64                     │  │
│  │  [الصورة الرمزية 3D] ← أوضاع لغة الإشارة                  │  │
│  │  [Zustand Store] ← الحالة العامة للتطبيق                   │  │
│  └───────────────────────┬─────────────────────────────────────┘  │
└──────────────────────────┼──────────────────────────────────────┘
                           │
           ┌───────────────┴─────────────────────┐
           │  REST API + WebSocket               │
           │  HTTP: /api/v1/*                    │
           │  WS: /api/v1/ws/session/{id}        │
           └───────────────┬─────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────────┐
│                   FastAPI - الجانب الخلفي                        │
│                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐  ┌──────────────┐  │
│  │ المصادقة │  │ الجلسات  │  │ STT الكلام │  │ TTS الصوت   │  │
│  │ /auth/*  │  │ /sessions│  │ /speech-   │  │ /text-to-   │  │
│  │          │  │          │  │ to-text    │  │ speech       │  │
│  └──────────┘  └──────────┘  └─────┬──────┘  └──────┬───────┘  │
│                                     │                 │           │
│  ┌──────────────────────────────────┼─────────────────┼─────────┐│
│  │              خدمات الذكاء الاصطناعي               │         ││
│  │  ┌────────────┐  ┌────────────┐  ┌─────────────────────────┐ ││
│  │  │ STTService  │  │ TTSService  │  │   OpenRouterClient      │ ││
│  │  │  (Whisper) │  │ (EdgeTTS)  │  │  (Claude 3 Haiku)       │ ││
│  │  └──────┬─────┘  └──────┬─────┘  └──────────┬─────────────┘ ││
│  └─────────┼───────────────┼───────────────────┼───────────────┘│
│            │               │                   │                 │
│  ┌─────────▼───────────────▼───────────────────▼───────────────┐│
│  │               واجهات برمجة الذكاء الاصطناعي الخارجية        ││
│  │    OpenRouter (Whisper STT + Claude LLM) | Edge TTS           ││
│  └───────────────────────────────────────────────────────────────┘│
│                                                                   │
│  ┌──────────────────┐    ┌────────────────────┐                  │
│  │  SQLite / PgSQL   │    │   Redis (اختياري)   │                 │
│  │ (المستخدمون،      │    │  (بث WebSocket     │                 │
│  │  الرسائل،         │    │   متعدد الخوادم)    │                 │
│  │  الجلسات)         │    │                    │                  │
│  └──────────────────┘    └────────────────────┘                  │
└───────────────────────────────────────────────────────────────────┘
```

### مسار تدفق البيانات: تحويل الكلام إلى نص (STT)
```
المستخدم يتكلم بالقرب من الميكروفون
    → المتصفح يلتقط الصوت بصيغة WebM/Opus عبر MediaRecorder API
    → JavaScript يحوّل القطع إلى Base64
    → WebSocket: {type:"audio_chunk", data:<base64>}
    → الخادم يجمع القطع في الذاكرة
    → المستخدم يوقف التسجيل
    → WebSocket: {type:"audio_end"}
    → الخادم → STTService.transcribe()
    → OpenRouter API → Whisper
    → النتيجة: {text, language, segments}
    → الخادم: normalize_text() + detect_language()
    → الخادم: classify_text() لاكتشاف المشاعر والنوايا
    → إرسال: {type:"transcript_final", text, confidence, lang}
    → إرسال: {type:"sentiment", label, intent}
    → إرسال: {type:"ai_partial"} × N + {type:"ai_final"}
    → الواجهة الأمامية: عرض الترجمة في ChatTimeline
```

### مسار تدفق البيانات: تحويل النص إلى كلام (TTS)
```
المستخدم يكتب رسالة
    → يضغط "نطق بصوت عالٍ"
    → WebSocket: {type:"user_text", text, requestTTS:true}
    → الخادم: TTSService.synthesize(text, "ar")
    → Edge TTS → "ar-SA-ZariyahNeural" → بيانات MP3
    → تشفير Base64 → WebSocket: {type:"tts_ready", audio:<base64>}
    → المتصفح: base64 → blob URL → new Audio().play()
    → الكلام يُسمع للمحادث الآخر
```

---

## ٣. التقنيات المستخدمة

### تقنيات الجانب الأمامي (Frontend)

| التقنية | الإصدار | الاستخدام | سبب الاختيار |
|---|---|---|---|
| **Next.js** | 16.x | إطار React مع App Router | التوجيه التلقائي، التصيير من الخادم |
| **React** | 19.x | مكتبة واجهة المستخدم | النظام البيئي الواسع، Hooks |
| **TypeScript** | 5.8 | JavaScript مع الأنواع | منع الأخطاء، دعم IDE أفضل |
| **Tailwind CSS** | 3.4 | إطار CSS مرن | تطوير UI سريع ومتسق |
| **Framer Motion** | 12.x | مكتبة الحركة والانيميشن | حركات سلسة ومتاحة |
| **Three.js** | 0.183 | محرك الرسومات ثلاثية الأبعاد | الصورة الرمزية ثلاثية الأبعاد |
| **@react-three/fiber** | 9.x | React مع Three.js | إدارة المشهد ثلاثي الأبعاد |
| **Zustand** | 5.x | إدارة الحالة | بسيط وفعّال |
| **Lucide React** | 0.487 | مكتبة الأيقونات | أيقونات SVG متاحة |

### تقنيات الجانب الخلفي (Backend)

| التقنية | الإصدار | الاستخدام | سبب الاختيار |
|---|---|---|---|
| **FastAPI** | 0.111+ | إطار الويب بلغة Python | دعم async، WebSocket، توثيق تلقائي |
| **Uvicorn** | 0.30+ | خادم ASGI | خادم إنتاجي للتطبيقات غير المتزامنة |
| **Pydantic v2** | 2.7+ | التحقق من البيانات | أمان الأنواع لطلبات/استجابات API |
| **SQLAlchemy** | 2.0 async | ORM لقاعدة البيانات | دعم async ومتوافق مع SQLite وPostgreSQL |
| **Alembic** | 1.13 | إدارة ترقيات قاعدة البيانات | نسخ المخطط وترقياته |
| **bcrypt** | 4.1 | تشفير كلمات المرور | معيار صناعي آمن |
| **python-jose** | 3.3 | JWT | مصادقة آمنة بالرموز |
| **openai** | 1.35 | SDK لـ OpenAI | الوصول لنموذج Whisper STT |
| **edge-tts** | 6.1 | Microsoft Edge TTS | أصوات عربية وإنجليزية مجانية |
| **slowapi** | 0.1.9 | تحديد معدل الطلبات | حماية نقاط الواجهة من الإساءة |
| **structlog** | 24.2 | سجلات منظمة (Logs) | سجلات JSON في الإنتاج |
| **redis** | 5.0 | عميل Redis | بث WebSocket لخوادم متعددة |

### الذكاء الاصطناعي والـ APIs الخارجية

| الخدمة | المزوّد | الاستخدام |
|---|---|---|
| **Whisper** | OpenAI (عبر OpenRouter) | تحويل الكلام إلى نص |
| **gpt-audio-mini** | OpenAI (عبر OpenRouter) | STT + TTS بديل |
| **Claude 3 Haiku** | Anthropic (عبر OpenRouter) | المحادثة، التبسيط، الترجمة |
| **gpt-4o-mini** | OpenAI (عبر OpenRouter) | تصنيف النصوص (مشاعر/نوايا) |
| **Edge TTS** | Microsoft (مجاني) | تحويل النص إلى كلام |
| **ElevenLabs** | ElevenLabs (اختياري) | أصوات TTS متميزة |

---

## ٤. توثيق الـ Backend الخلفي

### هيكل ملفات الخادم
```
backend/
├── app/
│   ├── main.py                     ← نقطة الدخول الرئيسية
│   ├── api/v1/
│   │   ├── router.py               ← تجميع المسارات
│   │   └── endpoints/
│   │       ├── auth.py             ← تسجيل/دخول/خروج
│   │       ├── users.py            ← بيانات المستخدم
│   │       ├── sessions.py         ← إدارة الجلسات
│   │       ├── match.py            ← المطابقة التلقائية
│   │       ├── speech_to_text.py   ← نقطة STT
│   │       ├── text_to_speech.py   ← نقطة TTS
│   │       ├── ai_response.py      ← نقطة اللغة الذكية
│   │       ├── ws_session.py       ← معالج WebSocket الرئيسي
│   │       └── health.py           ← صحة الخادم / القياسات
│   ├── core/
│   │   ├── config.py               ← الإعدادات من .env
│   │   ├── security.py             ← JWT + bcrypt
│   │   ├── rate_limit.py           ← تحديد معدل الطلبات
│   │   ├── metrics.py              ← قياسات الأداء
│   │   └── logging_config.py       ← إعداد structlog
│   ├── models/                     ← نماذج SQLAlchemy ORM
│   ├── schemas/                    ← مخططات Pydantic
│   ├── crud/                       ← عمليات قاعدة البيانات
│   ├── services/                   ← خدمات الذكاء الاصطناعي
│   ├── realtime/room_manager.py    ← سجل اتصالات WebSocket
│   └── db/                         ← الجلسة، القاعدة، البذر
├── alembic/                        ← نصوص ترقية قاعدة البيانات
├── tests/                          ← اختبارات Pytest
└── requirements.txt
```

### نقاط واجهة برمجة التطبيقات (API Endpoints)

| نقطة الوصول | الأسلوب | الغرض | يتطلب مصادقة | تحديد المعدل |
|---|---|---|---|---|
| `/api/v1/health` | GET | فحص صحة الخادم وحالة الخدمات | لا | افتراضي |
| `/api/v1/metrics` | GET | قياسات الأداء | لا | 30/دقيقة |
| `/api/v1/auth/register` | POST | إنشاء حساب مستخدم | لا | 20/دقيقة |
| `/api/v1/auth/login` | POST | المصادقة واستلام JWT | لا | 30/دقيقة |
| `/api/v1/auth/logout` | POST | تسجيل الخروج | لا | افتراضي |
| `/api/v1/users/me` | GET | جلب الملف الشخصي | نعم (JWT) | افتراضي |
| `/api/v1/sessions` | POST | إنشاء جلسة محادثة | نعم (JWT) | افتراضي |
| `/api/v1/sessions/join` | POST | الانضمام بكود دعوة | نعم (JWT) | افتراضي |
| `/api/v1/sessions/{id}/messages` | GET | جلب رسائل الجلسة | نعم (JWT) | افتراضي |
| `/api/v1/sessions/{id}/ws-ticket` | POST | إصدار تذكرة WebSocket | نعم (JWT) | افتراضي |
| `/api/v1/match/enqueue` | POST | الدخول لقائمة المطابقة | نعم (JWT) | افتراضي |
| `/api/v1/match/poll` | GET | استطلاع نتيجة المطابقة | نعم (JWT) | افتراضي |
| `/api/v1/match/dequeue` | DELETE | الخروج من قائمة المطابقة | نعم (JWT) | افتراضي |
| `/api/v1/speech-to-text` | POST | تحويل الصوت إلى نص | لا | 30/دقيقة |
| `/api/v1/text-to-speech` | POST | تحويل النص إلى صوت | لا | 30/دقيقة |
| `/api/v1/voices` | GET | قائمة الأصوات المتاحة | لا | 10/دقيقة |
| `/api/v1/ai-response` | POST | استجابة اللغة الذكية | لا | 30/دقيقة |
| `/api/v1/action` | POST | تنفيذ إجراء (تبسيط/ترجمة) | لا | 20/دقيقة |
| `/api/v1/ws/session/{id}` | WebSocket | جلسة الوقت الفعلي | اختياري (JWT) | - |

### أحداث WebSocket

**الأحداث الواردة من المتصفح:**

| نوع الحدث | البيانات | الوصف |
|---|---|---|
| `audio_chunk` | `{data: base64, mimeType}` | إرسال قطعة صوتية |
| `audio_end` | `{lang}` | انتهاء التسجيل، بدء المعالجة |
| `user_text` | `{text, requestTTS, lang}` | إرسال رسالة نصية |
| `action` | `{action, text, messageId, targetLang}` | تنفيذ إجراء على رسالة |
| `ping` | - | للحفاظ على الاتصال |

**الأحداث الصادرة إلى المتصفح:**

| نوع الحدث | البيانات | الوصف |
|---|---|---|
| `status` | `{state}` | حالة النظام (idle/listening/processing/speaking) |
| `transcript_final` | `{text, confidence, lang, messageId}` | نتيجة التحويل النهائية |
| `sentiment` | `{label, intent, confidence}` | تصنيف المشاعر والنوايا |
| `ai_partial` | `{text, messageId}` | استجابة الذكاء الاصطناعي الجارية |
| `ai_final` | `{text, messageId}` | استجابة الذكاء الاصطناعي الكاملة |
| `tts_ready` | `{audio, duration, voice}` | ملف الصوت المُوَلَّد |
| `ai_enhancement` | `{text, messageId}` | النص المُحسَّن بالذكاء الاصطناعي |
| `sign_suggestion` | `{phraseKey, assetUrl}` | اقتراح لغة إشارة |
| `message` | `{kind, text, senderId}` | رسالة من مشارك آخر (وضع الغرفة) |
| `error` | `{message, code}` | إشعار بخطأ |
| `pong` | - | رد على ping |

### الخدمات الرئيسية في الخادم

**خدمة تحويل الكلام إلى نص (STTService)**
- تدعم ثلاثة مزوّدين: `openrouter` (الافتراضي)، `gpt-audio-mini`، `openai`
- سلسلة الاحتياط تلقائية: Whisper → gpt-4o-mini-transcribe → gpt-audio-mini
- تحويل الصوت: FFmpeg يُحوّل webm إلى wav عند الحاجة

**خدمة تحويل النص إلى كلام (TTSService)**
- تدعم ثلاثة مزوّدين: `edge` (الافتراضي)، `gpt-audio-mini`، `elevenlabs`
- أصوات عربية: `ar-SA-ZariyahNeural` (أنثوي)، `ar-SA-HamedNeural` (ذكوري)
- أصوات إنجليزية: `en-US-JennyNeural` (أنثوي)، `en-US-AndrewNeural` (ذكوري)
- الاحتياط: فشل Edge TTS → gpt-audio-mini تلقائياً

**عميل OpenRouter (OpenRouterClient)**
- `stream_chat()`: يبثّ الرموز من OpenRouter عبر SSE
- `stream_action()`: يطبّق قوالب مخصصة للتبسيط/الترجمة/التوضيح
- `complete_with_model()`: لمهام التصنيف السريعة (gpt-4o-mini)
- نظام المطالبة (System Prompt): مُصمَّم خصيصاً لمساعدة الصم والبكم

**خدمة اكتشاف اللغة (LanguageDetector)**
- يعتمد على نطاقات Unicode: عربي (U+0600-U+06FF) مقابل لاتيني (a-zA-Z)
- دقة التشخيص تزيد عند تجاوز 60% من الأحرف
- حساب درجة إمكانية القراءة (0-100) بناءً على طول الكلمات والجمل

**خدمة الذكاء الاصطناعي السياقي (ai_context)**
- `classify_text()`: يصنّف المشاعر (happy/sad/angry/neutral/anxious) والنوايا (question/statement/urgent/other)
- `enhance_text()`: يُعيد صياغة الرسالة لتكون واضحة وصديقة للصم

---

## ٥. توثيق الـ Frontend الأمامي

### هيكل الصفحات

| الصفحة | المسار | الغرض | الـ API المستخدمة |
|---|---|---|---|
| **الصفحة الرئيسية** | `/` | الصفحة التسويقية مع العرض التوضيحي | لا شيء |
| **تسجيل الدخول** | `/login` | مصادقة المستخدم | POST /auth/login |
| **إنشاء حساب** | `/signup` | تسجيل مستخدم جديد | POST /auth/register |
| **الردهة** | `/lobby` | إنشاء/انضمام/مطابقة جلسات | /sessions, /match/* |
| **المحادثة** | `/chat/[sessionId]` | غرفة دردشة مصادَق عليها | WS + /sessions/{id}/messages |
| **التطبيق** | `/app` | مساحة تجريبية بدون تسجيل | WS فقط |

### الحالة العامة (Zustand Store)

| حقل الحالة | النوع | الوصف |
|---|---|---|
| `sessionId` | string | معرف جلسة WebSocket الحالية |
| `messages` | ChatMessage[] | سجل كامل للمحادثة |
| `liveCaption` | string | الترجمة الجارية في الوقت الفعلي |
| `liveAiResponse` | string | استجابة الذكاء الاصطناعي الجارية |
| `systemStatus` | idle/listening/processing/speaking | الحالة التشغيلية الحالية |
| `isConnected` | boolean | حالة اتصال WebSocket |
| `language` | "en" / "ar" | اللغة المختارة للواجهة |
| `isHighContrast` | boolean | وضع التباين العالي |
| `fontSize` | normal/large/xlarge | حجم الخط (إمكانية الوصول) |
| `signPreview` | {phraseKey, assetUrl} | بطاقة اقتراح لغة الإشارة الحالية |

### الصورة الرمزية ثلاثية الأبعاد (HologramSigner3D)

مبنية باستخدام Three.js وReact Three Fiber. تشمل:
- **ثمانية أوضاع للإشارة**: neutral, wave, thank-you, yes, no, please, help, question
- **انتقال سلس**: يستخدم `MathUtils.damp()` للتحريك السلس بين الأوضاع
- **روبوت إجرائي**: مبني من هندسة Three.js (capsule, sphere, cylinder)
- **تفاصيل بصرية**: توهج سماوي هولوغرامي، حلقة دوّارة، جسيمات متلألئة
- **تحميل نموذج واقعي**: يتحقق من وجود `/models/signer.glb` ويحمله إن وُجد

### ميزات إمكانية الوصول

- **وضع التباين العالي**: تبديل متغيرات CSS لاستيفاء معيار WCAG AAA
- **تكبير الخط**: ثلاثة مستويات (عادي، كبير، أكبر)
- **تسميات ARIA**: كل عنصر تفاعلي لديه aria-label وصفي
- **التنقل بلوحة المفاتيح**: Tab/Enter مع حلقات تركيز مرئية
- **مناطق ARIA الحية**: إعلانات بتغييرات الحالة لقارئات الشاشة
- **دعم RTL**: تخطيط النص العربي من اليمين إلى اليسار
- **الحركة المخفّفة**: `useSafeReducedMotion` تعطّل الحركات عند الحاجة

---

## ٦. توثيق نماذج الذكاء الاصطناعي

### ملخص مكونات الذكاء الاصطناعي

يستخدم النظام ثلاثة أنواع من الذكاء الاصطناعي:
1. **تحويل الكلام إلى نص (STT)**: OpenAI Whisper
2. **تحويل النص إلى كلام (TTS)**: Microsoft Edge TTS
3. **النموذج اللغوي الكبير (LLM)**: Anthropic Claude 3 Haiku عبر OpenRouter

---

### النموذج الأول: تحويل الكلام إلى نص (Whisper)

**اسم النموذج**: OpenAI Whisper-1
**نوع النموذج**: Transformer encoder-decoder متعدد اللغات
**المهمة**: التعرف التلقائي على الكلام (ASR)

**ما هو Whisper؟**
Whisper نموذج شبكة عصبية كبير درّبته OpenAI على 680,000 ساعة من الصوت متعدد اللغات. يعالج طيف الصوت (mel spectrogram) ثم يُولّد النص عبر فكّ الترميز الرمزي (token decoding). يدعم 99 لغة بما فيها العربية.

**كيفية إعداد المدخلات:**
1. المتصفح يلتقط الصوت بصيغة WebM/Opus عبر `MediaRecorder API`
2. JavaScript يحوّل كل قطعة إلى Base64 ويرسلها عبر WebSocket
3. الخادم يجمع القطع في ذاكرة مؤقتة (`audio_buffer: bytearray`)
4. عند انتهاء التسجيل، يُمرَّر الصوت الكامل إلى `STTService.transcribe()`

**كيفية تنفيذ الاستدلال (Inference):**
```python
response = await client.audio.transcriptions.create(
    model="openai/whisper-1",
    file=audio_file,              # BytesIO بامتداد .webm
    language=language_hint,       # مثلاً "ar" أو None للاكتشاف التلقائي
    response_format="verbose_json",
    timestamp_granularities=["segment"],
)
```

**معالجة المخرجات:**
- `response.text` → تطبيع النص (`normalize_text()`)
- `response.language` → رمز اللغة المكتشفة ("ar" أو "en")
- `response.segments` → طوابع زمنية لكل مقطع
- الثقة: 0.92 إذا توفرت مقاطع، 0.85 إذا لم تتوفر

**سبب اختيار Whisper:**
- الأفضل للتعرف على الكلام متعدد اللغات وخاصة العربية
- يتعامل مع الضجيج البيئي واللهجات وأنماط الكلام الطبيعية
- يعالج الجمل الكاملة لدقة أعلى مقارنة بـ STT التدفقي
- لا يتطلب إلا مفتاح API واحد عبر OpenRouter

**سلسلة الاحتياط:**
1. `openai/whisper-1` عبر OpenRouter (الأولي)
2. `openai/gpt-4o-mini-transcribe` (عند فشل Whisper)
3. `openai/gpt-audio-mini` عبر chat completions (إذا كان تنسيق الصوت متوافقاً)

---

### النموذج الثاني: تحويل النص إلى كلام (Edge TTS)

**اسم النموذج**: Microsoft Edge TTS Neural Voices
**المزوّد**: Microsoft (مجاني عبر مكتبة edge-tts)
**المهمة**: تحويل النص إلى صوت طبيعي

**الأصوات المستخدمة:**
- عربي أنثوي: `ar-SA-ZariyahNeural`
- عربي ذكوري: `ar-SA-HamedNeural`
- إنجليزي أنثوي: `en-US-JennyNeural`
- إنجليزي ذكوري: `en-US-AndrewNeural`

**كيف يعمل:**
```python
communicate = edge_tts.Communicate(text, "ar-SA-ZariyahNeural", rate="+0%")
buffer = io.BytesIO()
async for chunk in communicate.stream():
    if chunk["type"] == "audio":
        buffer.write(chunk["data"])
audio_b64 = base64.b64encode(buffer.getvalue()).decode("utf-8")
```

**سبب اختيار Edge TTS:**
- مجاني تماماً بدون مفتاح API
- أصوات عصبية عالية الجودة من Microsoft Azure
- دعم ممتاز للغة العربية
- زمن استجابة منخفض

---

### النموذج الثالث: النموذج اللغوي الكبير (Claude 3 Haiku)

**اسم النموذج**: `anthropic/claude-3-haiku`
**المزوّد**: Anthropic (عبر OpenRouter)
**النوع**: نموذج لغوي كبير (LLM) — Transformer decoder
**المهمة**: المحادثة والتبسيط والتوضيح والترجمة

**ما هو OpenRouter؟**
OpenRouter بوابة API موحّدة تتيح الوصول إلى نماذج متعددة (Claude، GPT-4، Mistral، Llama) بمفتاح API واحد. يستخدم نفس تنسيق OpenAI API.

**سبب اختيار Claude 3 Haiku:**
- سرعة استجابة عالية (مهم للمحادثة الفورية)
- جودة عالية في العربية والإنجليزية
- يتبع التعليمات بدقة (مهم لمهام التبسيط/التوضيح)
- تكلفة مناسبة للتفاعلات ذات الحجم الكبير

**نظام المطالبة (System Prompt):**
```
أنت مساعد ذكاء اصطناعي لتيسير التواصل لذوي الإعاقة السمعية والصوتية.
استخدم لغة واضحة وبسيطة ومباشرة.
تجنب المصطلحات المعقدة والعبارات الغامضة.
كن موجزاً — كل كلمة لها أهمية.
أجب بنفس لغة المستخدم ما لم يُطلب الترجمة.
```

**قوالب المهام:**

| المهمة | القالب |
|---|---|
| `simplify` (تبسيط) | "أعد كتابة النص بكلمات بسيطة جداً وجمل قصيرة. احتفظ بالمعنى الكامل." |
| `clarify` (توضيح) | "اشرح النص بوضوح. أضف سياقاً مفيداً. استخدم لغة بسيطة." |
| `translate_to_ar` | "ترجم إلى العربية. استخدم العربية الفصحى الواضحة الطبيعية." |
| `translate_to_en` | "Translate to English. Use clear, natural English." |

**التصنيف الذكي (Text Classification):**
- النموذج: `openai/gpt-4o-mini` (أسرع وأقل تكلفة للتصنيف)
- المخرجات: `{emotion, intent, urgency, confidence}` بصيغة JSON
- المشاعر: happy, sad, angry, neutral, anxious
- النوايا: question, statement, urgent, other

**البث المتدفق (Streaming):**
```
المتصفح ← رسالة WebSocket {type:"ai_partial", text:"النص يتزايد..."}
المتصفح ← رسالة WebSocket {type:"ai_final", text:"الاستجابة الكاملة"}
```

---

### أسئلة الدكتور عن نماذج الذكاء الاصطناعي

**س1: لماذا اخترتم Whisper لتحويل الكلام إلى نص؟**
ج: Whisper هو أفضل نموذج متاح للتعرف على الكلام متعدد اللغات، وخاصة العربية. دُرِّب على 680,000 ساعة من الصوت المتنوع بما يشمل اللهجات العربية. يستوعب الضجيج البيئي واللهجات وأنماط الكلام الطبيعية بشكل ممتاز. عبر OpenRouter، يحتاج فقط لمفتاح API واحد لتشغيل Whisper و Claude معاً.

**س2: ما نوع نموذج Whisper؟**
ج: Whisper هو نموذج Transformer encoder-decoder. يعالج الترميز الطيفي للصوت (mel spectrogram features) في جزء الترميز، ثم يولّد النص رمزاً بعد رمز في جزء فكّ الترميز. وهو نموذج seq2seq مشابه في بنيته لنماذج T5 وBART.

**س3: كيف يتلقى النموذج مدخلات الصوت؟**
ج: يلتقط المتصفح الصوت بصيغة WebM/Opus عبر `MediaRecorder API`. يُشفّر JavaScript الصوت كـ Base64 ويرسله عبر WebSocket. يجمع الخادم القطع في ذاكرة مؤقتة، وعند توقف التسجيل يمرّر البيانات الكاملة لـ STTService. يُغلَّف الصوت كملف BytesIO ويُرسَل إلى نقطة `/audio/transcriptions` في Whisper.

**س4: كيف يولّد النموذج مخرجاته؟**
ج: Whisper يولّد النص النهائي عبر Beam Search decoding. تعيد الـ API النص الكامل، واللغة المكتشفة، وطوابع زمنية اختيارية للمقاطع. يطبّع الخادم النص بعدها (إزالة أحرف التحكم، تطبيع Unicode)، ثم يشغّل اكتشاف اللغة الإضافي، وأخيراً يُرسل النتيجة عبر WebSocket.

**س5: ما هو درجة الثقة (Confidence)؟**
ج: لا يعيد Whisper API درجة ثقة أصلية لكل رمز. نستخدم درجة استدلالية: 0.92 إذا أعادت الاستجابة مقاطع زمنية (تعني تعرفاً تفصيلياً ناجحاً)، و0.85 إذا أعادت النص فقط. يُوضَّح ذلك للمستخدم كتقريب.

**س6: لماذا لا تستخدمون Google Speech-to-Text؟**
ج: ستتطلب Google STT إعداد حساب Google Cloud منفصل، مما يُعقّد النشر. OpenRouter يجمع جميع خدمات الذكاء الاصطناعي تحت مفتاح واحد. إضافة لذلك، Whisper يتفوق في الأداء متعدد اللغات وخاصة العربية مقارنة بـ Google STT وفق اختباراتنا.

**س7: ما المعالجة المسبقة المطلوبة للصوت؟**
ج: لا تتم معالجة مسبقة على جانب المتصفح. يستخدم MediaRecorder API الأصلي للمتصفح مباشرة. في الخادم، عند استخدام مسار gpt-audio-mini، يُحوّل FFmpeg صيغة WebM إلى WAV (PCM 16-bit) لأن ذلك النموذج يتطلب WAV أو MP3. Whisper يقبل WebM مباشرة.

**س8: ما الذي يحدث إذا أعطى النموذج نتيجة خاطئة؟**
ج: يرى المستخدم النص في سجل المحادثة ويمكنه استخدام زر "توضيح" للحصول على شرح أفضل، أو تصحيح النص يدوياً بالكتابة. النظام لا يصحّح تلقائياً أو يخفي النصوص الضعيفة — الشفافية ضرورية لإمكانية الوصول.

**س9: كيف يختلف استخدام LLM عن STT؟**
ج: STT (Whisper) يحوّل الصوت إلى نص — وهو مهمة إدراك حسي. LLM (Claude 3 Haiku) يُنجز مهام فهم اللغة: المحادثة، تبسيط اللغة المعقدة، توضيح الرسائل الغامضة، الترجمة بين العربية والإنجليزية. يخدم كل منهما غرضاً مختلفاً تماماً.

**س10: هل النماذج مدرَّبة أم مُدرَّبة مسبقاً؟**
ج: Whisper وClaude 3 Haiku كلاهما نماذج مُدرَّبة مسبقاً نستخدمها عبر API — لم يُجرَ أي ضبط دقيق (Fine-tuning) في هذا المشروع. تجلب هذه النماذج مليارات المعاملات من المعرفة المكتسبة، ونُوجّه سلوكها من خلال قوالب مطالبة مُصمَّمة بعناية.

**س11: ما الفرق بين التدريب والاستدلال في مشروعكم؟**
ج: مشروعنا يُنجز فقط الاستدلال (Inference) — نُرسل مدخلات إلى نماذج مُدرَّبة مسبقاً ونستلم مخرجات. لا نُدرّب أي نموذج. التدريب يتضمن التعلّم من بيانات ضخمة وتحديث أوزان النموذج، ويتطلب موارد GPU ضخمة وبيانات موسومة. الاستدلال يستخدم نموذجاً ثابت الأوزان للتنبؤ بمدخلات جديدة.

**س12: هل النتيجة حتمية (Deterministic)؟**
ج: ليس تماماً. Whisper نتائجه شبه حتمية لنفس الصوت. للـ LLM نستخدم temperature=0.7 للمحادثة (غير حتمي، أكثر إبداعاً) وtemperature=0.2 للتصنيف (شبه حتمي). هذا يعني أن نفس الرسالة قد تُنتج تبسيطات مختلفة قليلاً في كل مرة، لكن نتائج التصنيف ثابتة جداً.

**س13: كيف تتعاملون مع أخطاء الذكاء الاصطناعي؟**
ج: جميع استدعاءات AI مُغلَّفة في كتل try/except. تُسجَّل الأخطاء عبر structlog وتُرسَل رسالة واضحة للمستخدم: `{type:"error", message:"فشل الذكاء الاصطناعي", code:"ai_error"}`. الواجهة تعرض الخطأ وتعود لحالة الخمول (idle).

**س14: هل النظام قابل للتطوير (Scalable)؟**
ج: نعم. جميع استدعاءات AI غير متزامنة (async). يمكن معالجة جلسات متعددة بشكل متزامن. Redis pub/sub يُتيح التطوير الأفقي (عدة خوادم). تحديد المعدل يمنع استنفاذ حصة API. لأحمال أعلى، يمكن إضافة قائمة مهام (Celery) لمعالجة STT/TTS.

**س15: ما هي المخاوف الأخلاقية؟**
ج:
- البيانات الصوتية تُرسَل لـ APIs خارجية (OpenRouter, OpenAI) — يجب إعلام المستخدمين
- قد يُنتج LLM نصاً متحيزاً في السياقات الحساسة (طبي، قانوني)
- دقة اللهجات العربية: Whisper يعمل بشكل أفضل مع الفصحى؛ اللهجات قد تكون أقل دقة
- تمثيل لغة الإشارة: الصورة الرمزية تستخدم تقريبات إيماءات مبسّطة

**س16: كيف تحمون بيانات المستخدم؟**
ج:
- الصوت لا يُخزَّن أبداً — يُعالَج في الذاكرة ويُحذف
- الرسائل تُخزَّن في قاعدة البيانات فقط في وضع الغرفة المصادَق عليها
- JWT تنتهي صلاحيته (7 أيام للوصول، 15 دقيقة لـ WebSocket)
- مفاتيح API متغيرات بيئة وليست في الكود المصدري

**س17: لماذا OpenRouter بدلاً من Anthropic API مباشرة؟**
ج: OpenRouter يوفر عدة مزايا:
1. مفتاح API واحد لنماذج متعددة (Whisper + Claude + gpt-audio-mini)
2. نظام احتياط ومسار مدمج
3. تبديل النماذج بسهولة (تغيير متغير بيئة واحد)
4. فوترة ومراقبة موحّدة

**س18: كيف تعمل ميزة لغة الإشارة؟**
ج: تُطابق خدمة `sign_phrase_service.py` النص مع عبارات معروفة باستخدام تعبيرات regex. عند العثور على تطابق (مثل "how are you" وتنويعاتها)، تُنشأ بطاقة اقتراح إشارة وتُرسَل عبر WebSocket `{type:"sign_suggestion"}`. تعرض الواجهة بطاقة SVG، وتُوضَّح الإيماءة عبر الصورة الرمزية ثلاثية الأبعاد.

**س19: ما البيانات (Dataset) التي يستخدمها المشروع؟**
ج: لم تُستخدَم مجموعة بيانات مخصصة — جميع النماذج مُدرَّبة مسبقاً. جدول `sign_mappings` في قاعدة البيانات يمكن تعبئته بعبارات مرتبطة بالوسائط (حالياً عنصر نائب). لم يُجرَ أي تدريب للنموذج.

**س20: كيف يمكن تحسين النماذج في المستقبل؟**
ج:
- ضبط دقيق لـ Whisper على أنماط كلام خاصة بالصم (أبطأ، مجزّأ)
- تطبيق STT تدفقي (مثل Deepgram) للترجمة كلمة بكلمة
- إضافة توليد فيديو لغة الإشارة باستخدام نماذج Diffusion أو Motion Capture
- ضبط دقيق للنموذج اللغوي على مجموعات بيانات حوارية متخصصة بإمكانية الوصول

---

## ٧. توثيق قاعدة البيانات

### نوع قاعدة البيانات
- **التطوير**: SQLite (`hearme.db`) عبر `aiosqlite`
- **الإنتاج**: PostgreSQL عبر `asyncpg`
- **ORM**: SQLAlchemy 2.0 async مع `DeclarativeBase`
- **الترقيات**: Alembic (ترقية أولية واحدة تُنشئ كل الجداول)

### مخطط قاعدة البيانات

#### جدول `users` (المستخدمون)

| العمود | النوع | القيود | الوصف |
|---|---|---|---|
| `id` | UUID | PRIMARY KEY | معرف المستخدم |
| `email` | VARCHAR(320) | UNIQUE, INDEX, NOT NULL | بريد تسجيل الدخول |
| `password_hash` | VARCHAR(255) | NOT NULL | تشفير bcrypt للكلمة السرية |
| `display_name` | VARCHAR(120) | NOT NULL | الاسم الظاهر في المحادثة |
| `user_type` | VARCHAR(16) | NOT NULL | "deaf" أو "mute" أو "both" |
| `locale` | VARCHAR(32) | default "en" | تفضيل لغة الواجهة |
| `font_scale` | VARCHAR(16) | default "normal" | تفضيل حجم الخط |
| `high_contrast` | BOOLEAN | default false | وضع التباين العالي |
| `created_at` | TIMESTAMP | server_default now() | وقت إنشاء الحساب |

#### جدول `sessions` (الجلسات)

| العمود | النوع | القيود | الوصف |
|---|---|---|---|
| `id` | UUID | PRIMARY KEY | معرف الجلسة |
| `mode` | VARCHAR(16) | NOT NULL | "direct" أو "matched" |
| `invite_code` | VARCHAR(32) | UNIQUE, INDEX, nullable | كود الدعوة للانضمام |
| `created_by` | UUID | FK → users.id | منشئ الجلسة |
| `ai_assist_enabled` | BOOLEAN | default true | هل المساعد الذكي مفعّل |
| `created_at` | TIMESTAMP | server_default | وقت الإنشاء |
| `closed_at` | TIMESTAMP | nullable | وقت إغلاق الجلسة |

#### جدول `session_participants` (المشاركون)

| العمود | النوع | القيود | الوصف |
|---|---|---|---|
| `session_id` | UUID | PK، FK → sessions.id | جزء من المفتاح الأساسي المركّب |
| `user_id` | UUID | PK، FK → users.id | جزء من المفتاح الأساسي المركّب |
| `role` | VARCHAR(16) | NOT NULL | "deaf" أو "mute" |
| `joined_at` | TIMESTAMP | server_default | وقت الانضمام |
| `left_at` | TIMESTAMP | nullable | وقت المغادرة |

#### جدول `messages` (الرسائل)

| العمود | النوع | القيود | الوصف |
|---|---|---|---|
| `id` | UUID | PRIMARY KEY | معرف الرسالة |
| `session_id` | UUID | FK، INDEX | الجلسة المالكة |
| `sender_id` | UUID | FK، nullable | المُرسِل (null للذكاء الاصطناعي) |
| `kind` | VARCHAR(32) | NOT NULL | "transcript" أو "user_text" |
| `content_text` | TEXT | NOT NULL | محتوى الرسالة |
| `stt_provider` | VARCHAR(64) | nullable | مزوّد STT المستخدم |
| `created_at` | TIMESTAMP | INDEX | طابع زمني للرسالة |

#### جدول `message_ai_metadata` (بيانات الذكاء الاصطناعي للرسائل)

| العمود | النوع | الوصف |
|---|---|---|
| `message_id` | UUID (PK) | علاقة 1:1 مع الرسالة |
| `sentiment_label` | VARCHAR(32) | المشاعر المكتشفة |
| `intent` | VARCHAR(32) | النية المكتشفة |
| `llm_model` | VARCHAR(128) | النموذج المستخدم للتحسين |
| `enhancement_text` | TEXT | النسخة المُحسَّنة بالذكاء الاصطناعي |

#### جدول `sign_mappings` (تعيينات لغة الإشارة)

| العمود | النوع | الوصف |
|---|---|---|
| `id` | UUID | معرف التعيين |
| `phrase_key` | VARCHAR(256) | العبارة النصية للمطابقة |
| `locale` | VARCHAR(16) | رمز اللغة ("en" أو "ar") |
| `asset_url` | TEXT | رابط أو URI لوسائط الإشارة |
| `priority` | INTEGER | أولوية الترتيب |

### العلاقات بين الجداول

```
users ─────────────────────────────────────────────────┐
  │                                                     │
  │ created_by (1:N)       user_id (M:N عبر المشاركين) │
  ▼                                   ▼                 │
sessions ── session_participants ── users               │
  │                                                     │
  │ session_id (1:N)                                    │
  ▼                                                     │
messages ── message_ai_metadata (1:1)                   │
                                                        │
users ── match_queue (1:N) ─── (مطابقة → جلسة)        │
                                                        │
sign_mappings (مستقل، يُبحَث فيه بـ phrase_key + locale)
```

---

## ٨. توثيق الميزات

### جدول الميزات الكامل

| # | الميزة | المكونات التقنية | فائدة المستخدم |
|---|---|---|---|
| 1 | **تحويل الكلام إلى نص في الوقت الفعلي** | Whisper، WebSocket، MediaRecorder | المستخدمون الصم يقرؤون ما يقوله الآخرون |
| 2 | **تحويل النص إلى كلام** | Edge TTS، Base64، Web Audio API | المستخدمون البكم يُسمَع كلامهم |
| 3 | **مساعد المحادثة الذكي** | Claude 3 Haiku، SSE streaming | شريك محادثة ذكي |
| 4 | **تبسيط الرسائل** | LLM مع قالب التبسيط | إعادة كتابة النص المعقد بلغة بسيطة |
| 5 | **توضيح الرسائل** | LLM مع قالب التوضيح | شرح الرسائل الغامضة |
| 6 | **الترجمة عربي/إنجليزي** | LLM مع قالب الترجمة | ترجمة فورية بين اللغتين |
| 7 | **الترجمة الفورية (Live Captions)** | WebSocket، LiveCaptionPanel | عرض النص الجاري لحظياً |
| 8 | **اقتراحات لغة الإشارة** | sign_phrase_service، sign_mappings DB | بطاقات عبارات لغة الإشارة |
| 9 | **الصورة الرمزية ثلاثية الأبعاد** | Three.js، React Three Fiber | عرض بصري لأوضاع الإشارة |
| 10 | **مصادقة المستخدمين** | JWT، bcrypt، SQLAlchemy | هوية آمنة واستمرارية الجلسات |
| 11 | **إدارة الجلسات** | Sessions API، أكواد الدعوة | غرف محادثة دائمة متعددة المستخدمين |
| 12 | **نظام المطابقة** | MatchQueue، polling | مزاوجة تلقائية بين الصم والبكم |
| 13 | **واجهة ثنائية اللغة** | i18n (en.ts/ar.ts)، CSS RTL | دعم عربي أصيل |
| 14 | **وضع التباين العالي** | متغيرات CSS المخصصة | معيار WCAG AAA لإمكانية الوصول |
| 15 | **تكبير حجم الخط** | حالة Zustand، CSS | قابلية قراءة لضعاف البصر |
| 16 | **التنقل بلوحة المفاتيح** | ARIA، حلقات التركيز | مستخدمو قارئات الشاشة |
| 17 | **مزوّدو AI متعددون** | STT/TTS/LLM قابل للتكوين | مرونة وموثوقية |
| 18 | **تحديد معدل الطلبات** | SlowAPI | حماية API من الإساءة |
| 19 | **سجلات منظمة** | structlog | مراقبة الإنتاج |
| 20 | **قياسات الأداء** | MetricsCollector، /metrics | مراقبة التأخير ومعدلات الأخطاء |

---

## ٩. مسار تجربة المستخدم

### مسار المستخدم الأصم (خطوة بخطوة)

**شرح مبسّط:**
1. افتح HearMeAI في المتصفح
2. للتجربة، اذهب إلى `/app` — لا تسجيل مطلوب
3. يتكلم شخص قريب منك
4. اضغط زر "استمع" (أيقونة الميكروفون الخضراء)
5. تظهر ترجمة حية في لوحة الترجمة الفورية
6. عند الانتهاء، اضغط "إيقاف" — يظهر النص النهائي في المحادثة
7. إذا كانت الرسالة غير واضحة، اضغط "تبسيط" على أي رسالة
8. للرد، اكتب رسالتك في مربع النص واضغط "إرسال"
9. لجعل رسالتك مسموعة، اضغط "نطق بصوت عالٍ"

**شرح تقني للمطوّرين:**
1. صفحة `/app` → مكوّن `ChatWorkspace` يُحمَّل
2. خطاف `useSession()` ينشئ `SessionWebSocket`، يتصل بـ WebSocket
3. المستخدم يضغط الميكروفون → `useMediaRecorder.startRecording()` → `MediaRecorder.start(500ms)`
4. كل 500ms: `onDataAvailable` → Base64 encode → `ws.sendAudioChunk(base64, "audio/webm")`
5. الخادم: يجمع في `audio_buffer`، يُرسِل status "listening"
6. المستخدم يوقف → `ws.sendAudioEnd()` → `{type:"audio_end"}`
7. الخادم: `handle_audio_end()` → `STTService.transcribe()` → Whisper API
8. الخادم: `normalize_text()` → `detect_language()` → `classify_text()`
9. الخادم يُرسِل: `{type:"transcript_final"}` + `{type:"sentiment"}`
10. إذا كان AI Assist مفعّلاً: `stream_chat()` → رموز SSE
11. الخادم يبثّ: `{type:"ai_partial"}` × N → `{type:"ai_final"}`
12. الواجهة: `addMessage({role:"transcript"})` → `updateMessage(aiPartial)`
13. `ChatTimeline` يُعاد عرضه مع الرسائل الجديدة

### مسار المستخدم الأبكم (خطوة بخطوة)

1. المستخدم يفتح التطبيق ويكتب رسالة في `ControlDock`
2. يضغط "نطق بصوت عالٍ"
3. `sendText(text, requestTTS=true)` عبر WebSocket
4. الخادم: `TTSService.synthesize(text, "ar")` → Edge TTS
5. يعيد Base64 MP3 → `{type:"tts_ready", audio, duration}`
6. الواجهة: `base64ToAudioUrl()` → blob URL → `new Audio(url).play()`
7. يُسمَع الكلام لمحادث الآخر

### مسار الغرفة المصادَق عليها

1. المستخدم يسجّل دخوله أو ينشئ حساباً
2. يُوجَّه إلى `/lobby`
3. المستخدم A ينشئ جلسة → يحصل على كود دعوة (مثلاً "XYZQ-42")
4. المستخدم B يُدخل الكود في `/lobby` → ينضم بالدور المعاكس
5. كلا المستخدمين يحصلان على تذكرة WebSocket → يتصلان بنفس الغرفة
6. الرسائل تُحفَظ في قاعدة البيانات وتُبثّ لجميع المشاركين
7. اقتراحات لغة الإشارة تظهر في لوحة جانبية

---

## ١٠. دليل التثبيت والتشغيل

### المتطلبات الأساسية
- Node.js 20+
- Python 3.11+
- مفتاح OpenRouter API (متاح على openrouter.ai)
- (اختياري) Redis للتطوير متعدد الخوادم

### الطريقة الأولى: Docker Compose (الموصى بها)

```bash
# 1. نسخ ملفات البيئة
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env.local

# 2. تحرير backend/.env — أضف مفتاح API:
#    OPENROUTER_API_KEY=sk-or-v1-...

# 3. تشغيل جميع الخدمات
docker compose up

# 4. افتح المتصفح
# الواجهة الأمامية: http://localhost:3000
# الخادم الخلفي:  http://localhost:8000
```

### الطريقة الثانية: الإعداد اليدوي

**إعداد الخادم الخلفي:**
```bash
cd backend

# إنشاء بيئة Python افتراضية
python -m venv .venv
.venv\Scripts\activate        # Windows
source .venv/bin/activate     # Linux/Mac

# تثبيت المتطلبات
pip install -r requirements.txt

# إعداد البيئة
cp .env.example .env
# تحرير .env: OPENROUTER_API_KEY=sk-or-v1-...

# تشغيل ترقيات قاعدة البيانات
alembic upgrade head

# تشغيل الخادم
uvicorn app.main:app --reload --port 8000
```

**إعداد الواجهة الأمامية:**
```bash
cd frontend

# تثبيت حزم Node.js
npm install

# إعداد البيئة
cp .env.example .env.local
# تحرير .env.local:
#   NEXT_PUBLIC_API_URL=http://localhost:8000
#   NEXT_PUBLIC_WS_URL=ws://localhost:8000

# تشغيل خادم التطوير
npm run dev
# افتح http://localhost:3000
```

### متغيرات البيئة

**الخادم الخلفي (`backend/.env`):**
```bash
OPENROUTER_API_KEY=sk-or-v1-...     # مطلوب
OPENROUTER_MODEL=anthropic/claude-3-haiku
STT_PROVIDER=openrouter
TTS_PROVIDER=edge
DATABASE_URL=sqlite+aiosqlite:///./hearme.db
JWT_SECRET_KEY=مفتاح-سري-طويل-عشوائي
ALLOWED_ORIGINS=http://localhost:3000
DEBUG=false
```

**الواجهة الأمامية (`frontend/.env.local`):**
```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
```

### المشاكل الشائعة وحلولها

| المشكلة | السبب | الحل |
|---|---|---|
| `OPENROUTER_API_KEY is required` | مفتاح API مفقود | أضف المفتاح إلى `backend/.env` |
| فشل اتصال WebSocket | الخادم لا يعمل أو WS_URL خاطئ | تأكد من تشغيل الخادم على منفذ 8000 |
| "Audio exceeds 25MB limit" | تسجيل طويل جداً | سجّل مقاطع أقصر (<10 ثوانٍ) |
| صوت Edge TTS فارغ | مشكلة شبكة | اضبط `TTS_EDGE_FALLBACK_TO_GPT_AUDIO=true` |
| خطأ CORS | URL الواجهة غير مضمّن | أضف URL الواجهة لـ `ALLOWED_ORIGINS` |

---

## ١١. الأمان والتحقق من المدخلات

### أمان المصادقة
- **تشفير كلمة المرور**: SHA-256 pre-digest + bcrypt (12 جولة). خطوة SHA-256 تحل مشكلة حد 72 بايت لـ bcrypt
- **JWT**: خوارزمية HS256، انتهاء صلاحية 7 أيام للوصول، 15 دقيقة لتذاكر WebSocket
- **أمان WebSocket**: تذكرة JWT قصيرة العمر مُتحقَّق منها قبل ترقية الاتصال
- **تسجيل الخروج بدون حالة**: JWT تُتجاهل من جانب العميل

### أمان الـ API
- **تحديد المعدل**: SlowAPI بحدود على أساس IP
- **CORS**: قائمة أصول مسموح بها قابلة للتكوين
- **رؤوس الأمان**: `X-Frame-Options: DENY`، `X-Content-Type-Options: nosniff`
- **توثيق API مخفي في الإنتاج**: `docs_url=None` ما لم يكن `DEBUG=true`

### التحقق من المدخلات
- **رفع الملفات الصوتية**: قائمة بيضاء لأنواع MIME، حد 25MB، حد أدنى 100 بايت
- **مدخلات النص**: تحقق Pydantic Schema لجميع أجسام الطلبات
- **أكواد الدعوة**: تحقق regex `^[A-Z0-9_]{4,32}$`
- **حقن SQL**: SQLAlchemy ORM مع استعلامات مُعاملات — لا SQL خام

### ما يمكن تحسينه
- إضافة قائمة رفض JWT على أساس Redis للخروج الحقيقي من الخادم
- إضافة تطبيق HTTPS (يُعالَج على مستوى منصة النشر)
- إضافة سجل مراجعة للعمليات الإدارية
- تطبيق حماية CSRF لنقاط REST المعدِّلة للجلسة

---

## ١٢. الاختبار والتصحيح

### الاختبارات الموجودة

```bash
cd backend
python -m pytest tests/ -v

# الاختبارات المتاحة:
# test_api_health.py       — اختبار نقطة /health
# test_context_manager.py  — اختبار إدارة سياق المحادثة
# test_language_detector.py — اختبار اكتشاف اللغة
```

### اختبار يدوي لـ STT

```bash
curl -X POST http://localhost:8000/api/v1/speech-to-text \
  -F "audio=@audio.webm;type=audio/webm" \
  -F "language=ar"
```

### اختبار يدوي لـ TTS

```bash
curl -X POST http://localhost:8000/api/v1/text-to-speech \
  -H "Content-Type: application/json" \
  -d '{"text": "مرحباً بالعالم", "language": "ar"}'
```

### اختبار WebSocket

```bash
# npm install -g wscat
wscat -c "ws://localhost:8000/api/v1/ws/session/test-123"
# أكتب: {"type":"ping"}
# المتوقع: {"type":"pong"}
```

### فحص الواجهة الأمامية

```bash
cd frontend
npm run type-check   # TypeScript
npm run lint         # ESLint
npm run build        # فحص بناء الإنتاج
```

---

## ١٣. التحديات والحلول

| التحدي | الحل | لماذا هذا النهج |
|---|---|---|
| **بث الصوت في الوقت الفعلي** | قطع WebSocket ثنائية + مخزن BytesIO | WebSocket يوفر اتصالاً دائماً؛ التخزين المؤقت يتجنب الإرسال الجزئي |
| **جودة TTS العربية** | Edge TTS مع `ar-SA-ZariyahNeural` | أفضل أصوات عصبية عربية من Microsoft Azure؛ مجانية |
| **اكتشاف اللغة العربية** | توصيف Unicode (U+0600-U+06FF) | نطاقات Unicode للعربية محددة جيداً؛ سريع وموثوق |
| **STT/TTS متعدد المزوّدين** | نمط الاستراتيجية مع تجريد المزوّد | واجهة واحدة مع تطبيقات قابلة للتكوين |
| **توافق تنسيقات الصوت** | تكامل FFmpeg | المتصفح يرسل WebM/Opus؛ بعض APIs تحتاج WAV/MP3 |
| **إعادة اتصال WebSocket** | تراجع أُسّي مع 5 محاولات كحد أقصى | يتعامل مع مشاكل الشبكة المؤقتة دون إغراق الخادم |
| **بث استجابات AI** | SSE عبر StreamingResponse | دعم أصلي لبث الرموز مع نوع MIME `text/event-stream` |
| **الصورة الرمزية ثلاثية الأبعاد بدون نموذج** | روبوت إجرائي Three.js مع انتقال الأوضاع | لا اعتماد على ملفات خارجية؛ يعمل دائماً مع حركات سلسة |
| **جلسات WebSocket المتزامنة** | سجل السياق لكل جلسة + Redis اختياري | كفاءة في الذاكرة؛ Redis يُتيح التوسع الأفقي |

---

## ١٤. القيود الحالية

### القيود التقنية
- **STT ليس وقت فعلي حقيقي**: Whisper يعالج الجمل الكاملة، المستخدمون ينتظرون توقف الكلام
- **لا تخزين صوتي**: الصوت يُعالَج في الذاكرة ويُحذف. لا توجد إمكانية إعادة التشغيل
- **قيود SQLite**: قارئ واحد عند التحميل العالي، يجب استخدام PostgreSQL
- **Redis اختياري**: بدون Redis، البث عبر WebSocket يعمل على خادم واحد فقط

### قيود النماذج
- **Whisper**: قد يواجه صعوبة مع اللهجات القوية والضجيج الشديد. دقة اللهجات العربية أقل من الفصحى
- **Edge TTS**: يحتاج اتصال بالإنترنت. لا تحكم دقيق في النبرة والإيقاع
- **LLM (Claude 3 Haiku)**: قد يفرط أحياناً في التبسيط مما يغيّر المعنى قليلاً

### قيود الواجهة
- **الصورة الرمزية**: تستخدم أوضاع إجرائية مبسّطة، ليست لغة إشارة كاملة. 8 أوضاع فقط
- **لغة الإشارة**: اقتراحات على مستوى العبارة فقط، لا رسوم متحركة كاملة للجملة
- **الجوال**: مُحسَّن للحاسوب؛ تجربة الجوال وظيفية لكن غير مُصقَّلة بالكامل

### قيود النشر
- **التوسع الرأسي**: خادم خلفي واحد بدون Redis
- **لا CDN**: بيانات الصوت تُرسَل عبر WebSocket، لا عبر CDN

---

## ١٥. العمل المستقبلي

| الأولوية | الميزة | المنهج التقني |
|---|---|---|
| عالية | **STT تدفقي حقيقي** | Deepgram أو AssemblyAI للترجمة كلمة بكلمة |
| عالية | **رسوم متحركة لغة إشارة كاملة** | بيانات Motion Capture + نموذج تقدير الأوضاع |
| عالية | **تطبيق جوال أصيل** | React Native مع نفس الخادم |
| متوسطة | **وضع غير متصل** | Whisper.cpp (نموذج محلي) + TTS محلي |
| متوسطة | **لوحة تحكم إدارية** | تحليلات الاستخدام، إدارة المستخدمين |
| متوسطة | **ضبط دقيق للنموذج** | Fine-tune على بيانات التواصل الخاصة بإمكانية الوصول |
| منخفضة | **المزيد من اللغات** | دعم الفرنسية، الإسبانية |
| منخفضة | **مزامنة التفضيلات** | تخزين تفضيلات إمكانية الوصول في قاعدة البيانات |

---

## ١٦. أسئلة الدكتور المتوقعة

### أسئلة المعمارية

**س: ما المعمارية الإجمالية لمشروعكم؟**
ج: المشروع عبارة عن monorepo يحتوي على مكوّنين رئيسيين: واجهة أمامية بـ Next.js 14 (React، TypeScript، Tailwind)، وخادم خلفي بـ FastAPI (Python async). تتواصل الواجهة مع الخادم عبر REST API لعمليات CRUD وعبر WebSocket للتواصل الفوري. يدمج الخادم ثلاث خدمات ذكاء اصطناعي: OpenAI Whisper للتعرف على الكلام، وMicrosoft Edge TTS لتوليد الصوت، وOpenRouter (Claude 3 Haiku) للذكاء اللغوي. تُخزَّن البيانات في SQLite/PostgreSQL عبر SQLAlchemy. Redis يُستخدَم اختيارياً لبث WebSocket متعدد الخوادم.

**س: لماذا اخترتم FastAPI للخادم الخلفي؟**
ج: FastAPI هو الخيار الأمثل لثلاثة أسباب: أولاً، دعم async كامل (مبني على Starlette/asyncio) ضروري لمعالجة اتصالات WebSocket المتزامنة واستدعاءات AI غير المتزامنة. ثانياً، دعم WebSocket أصلي. ثالثاً، تكامل Pydantic v2 يوفر التحقق التلقائي من صحة الطلبات والاستجابات. إضافة لذلك، يولّد توثيق OpenAPI تلقائياً في وضع التطوير، وهو أسرع بكثير من Flask أو Django للعمل غير المتزامن.

**س: لماذا WebSocket بدلاً من REST polling؟**
ج: WebSocket يوفر اتصالاً ثنائي الاتجاه دائماً، وهو ضروري للتواصل الفوري. REST polling سيُضيف تأخيراً (الواجهة ستحتاج للاستعلام كل بضع مئات من الميلي ثانية)، يُهدر النطاق الترددي، ويخلق حملاً زائداً على الخادم. WebSocket يسمح للخادم بدفع الترجمات واستجابات AI وصوت TTS فور جهوزيتها دون أن يطلب العميل ذلك.

**س: كيف تعمل إدارة الحالة في الواجهة الأمامية؟**
ج: نستخدم Zustand، مكتبة إدارة حالة React خفيفة الوزن. يحتفظ `sessionStore` واحد بجميع حالة التشغيل: الرسائل، نص الترجمة الحية، حالة النظام، تفضيل اللغة، إعدادات إمكانية الوصول. Zustand أبسط من Redux (لا actions/reducers/dispatch) مع توفير حالة مركزية. المكوّنات تشترك في شرائح محددة من المخزن لتجنب إعادة الرسم غير الضرورية.

**س: كيف تعمل المصادقة في مشروعكم؟**
ج: نستخدم JWT (JSON Web Tokens). عند تسجيل الدخول، ينشئ الخادم JWT مُوقَّع بـ HS256 باستخدام مفتاح سري. يحتوي JWT على معرف المستخدم ووقت الانتهاء (7 أيام). العميل يخزّنه في localStorage ويرسله كـ Bearer token في Authorization header. لاتصالات WebSocket، نستخدم تذكرة JWT قصيرة العمر (15 دقيقة) تحتوي أيضاً على معرف الجلسة.

### أسئلة نموذج الذكاء الاصطناعي

**س: ما أهم مكوّن ذكاء اصطناعي في مشروعكم؟**
ج: مكوّن تحويل الكلام إلى نص (Whisper) هو الأكثر أهمية لأنه الجسر التواصلي الرئيسي للمستخدمين الصم — بدون ترجمة دقيقة يفشل حالة الاستخدام الأساسية. LLM (Claude 3 Haiku) يأتي ثانياً لأنه يعالج تبسيط النص والمساعدة الذكية. TTS يأتي ثالثاً، يخدم أساساً المستخدمين البكم.

**س: هل يمكن أن يعمل المشروع بدون مفتاح API؟**
ج: جزئياً. Edge TTS يعمل بدون أي مفتاح API. الصفحة الرئيسية والمصادقة وإدارة الجلسات الأساسية تعمل بدون مفتاح. ولكن تحويل الكلام إلى نص (Whisper) ونموذج LLM (Claude 3 Haiku) كلاهما يتطلبان مفتاح OpenRouter API. بدون المفتاح، لن تعمل ميزات "الاستماع" و"المساعد الذكي".

**س: ما الذي يُميّز مشروعكم عن Google Translate + Google STT؟**
ج: ثلاثة فوارق رئيسية: (1) التكامل — جميع الميزات تعمل معاً بسلاسة في واجهة فورية واحدة. (2) تصميم مُوجَّه بإمكانية الوصول — امتثال WCAG، تباين عالٍ، تكبير خط، تنقل بلوحة مفاتيح. (3) اقتراحات لغة الإشارة والصورة الرمزية ثلاثية الأبعاد لسياق لغة الإشارة. (4) استمرارية محادثة ثنائية اللغة مع تصنيف وتحسين AI محفوظ في قاعدة البيانات.

### أسئلة قاعدة البيانات

**س: لماذا SQLite للتطوير وPostgreSQL للإنتاج؟**
ج: SQLite لا تتطلب أي إعداد للتطوير المحلي — مجرد ملف واحد (`hearme.db`). PostgreSQL تتعامل مع الكتابات المتزامنة بشكل أفضل، أداء أقوى على نطاق واسع، ومدعومة من المنصات السحابية. SQLAlchemy يستخدم مُشغّلات async لكليهما: `aiosqlite` لـ SQLite و`asyncpg` لـ PostgreSQL. نفس كود ORM يعمل مع كلتا قاعدتي البيانات.

**س: ما الذي يُخزَّن في قاعدة البيانات مقابل الذاكرة؟**
ج: في الذاكرة: مخازن الصوت المؤقتة لـ WebSocket، سياق المحادثة (آخر 20 رسالة)، اتصالات غرفة WebSocket. في قاعدة البيانات: المستخدمون، الجلسات، نصوص الرسائل، بيانات AI الوصفية (المشاعر، النوايا، التحسينات)، تعيينات الإشارة. الصوت نفسه لا يُخزَّن أبداً — يُعالَج في الذاكرة ثم يُحذف.

### أسئلة الأمان

**س: كيف تُؤمِّنون اتصال WebSocket؟**
ج: للجلسات المصادَق عليها، يجب على العملاء أولاً استدعاء `POST /sessions/{id}/ws-ticket` للحصول على JWT قصير العمر (15 دقيقة). تُمرَّر هذه التذكرة كمعامل query عند الاتصال بـ WebSocket. الخادم يتحقق من توقيع JWT، ويتحقق أن نوع الرمز "ws" (وليس "access")، ويتحقق من مطابقة معرف الجلسة، ويؤكد أن المستخدم مشارك. الرموز غير الصالحة تُغلق WebSocket بكود 4401 (غير مصرح) أو 4403 (محظور).

**س: كيف تمنعون حقن SQL؟**
ج: نستخدم SQLAlchemy ORM حصراً. جميع عمليات قاعدة البيانات تستخدم استعلامات مُعلَّمة مع معاملات مرتبطة. لا تُبنى سلاسل SQL خام من مدخلات المستخدم. هجرات Alembic تستخدم أيضاً بيانات ORM، لا SQL خام.

---

## ١٧. نصوص العرض التقديمي

### الشرح في دقيقة واحدة (عربي)
"HearMeAI هو تطبيق ذكاء اصطناعي لمساعدة الصم والبكم على التواصل. يحوّل الكلام إلى نص فوري للصم باستخدام نموذج Whisper من OpenAI، ويحوّل النص إلى كلام مسموع للبكم باستخدام Microsoft Edge TTS، ويوفّر مساعداً ذكياً لتبسيط الرسائل وترجمتها باستخدام Claude من Anthropic. يدعم التطبيق العربية والإنجليزية، ومُصمَّم بأعلى معايير إمكانية الوصول، مع واجهة ثلاثية الأبعاد للغة الإشارة. الخادم FastAPI بلغة Python، والواجهة Next.js بـ TypeScript، والتواصل فوري عبر WebSocket."

### الشرح في 3 دقائق (عربي)
"يعاني أكثر من 430 مليون شخص حول العالم من ضعف السمع، ويعجز ملايين آخرون عن الكلام. هؤلاء يواجهون حواجز تواصل يومية تُعيق حياتهم.

مشروعنا HearMeAI يحل هذه المشكلة عبر ثلاث قنوات ذكاء اصطناعي. الأولى: تحويل الكلام إلى نص — باستخدام نموذج Whisper من OpenAI، نُترجم الكلام المنطوق إلى نص فوري ظرف ثانية أو ثانيتين. المستخدم الأصم يرى ما يقوله الآخرون في الوقت ذاته. الثانية: تحويل النص إلى كلام — المستخدم الأبكم يكتب رسالته ويضغط 'نطق بصوت عالٍ'، فتُحوَّل إلى صوت طبيعي بأصوات عربية وإنجليزية عبر Microsoft Edge TTS. الثالثة: المساعد الذكي — باستخدام Claude 3 Haiku من Anthropic عبر OpenRouter، يمكن تبسيط الرسائل المعقدة، وتوضيح الغامض، وترجمة الجمل بين العربية والإنجليزية.

الخادم الخلفي بلغة Python مع FastAPI وهو async بالكامل، يدعم WebSocket للبث الفوري. الواجهة الأمامية Next.js 14 مع TypeScript وتتميز بصورة رمزية ثلاثية الأبعاد تُظهر أوضاع لغة الإشارة. قاعدة البيانات SQLite للتطوير وPostgreSQL للإنتاج مع مصادقة JWT آمنة."

### الشرح في 5 دقائق (عربي)
[ابدأ بشرح 3 دقائق ثم أكمل:]

"دعونا نستعرض المعمارية بمزيد من التفصيل. عندما يتكلم المستخدم، يلتقط المتصفح الصوت بصيغة WebM عبر MediaRecorder API ويبثّه عبر WebSocket للخادم. يجمع الخادم قطع الصوت في الذاكرة، وعند توقف التسجيل يرسلها لـ OpenRouter's Whisper. يُعيد Whisper النص واللغة المكتشفة والطوابع الزمنية. يُشغّل الخادم ثم عمليتَي ذكاء اصطناعي: تصنيف النص باستخدام gpt-4o-mini لاكتشاف المشاعر والنوايا، وإذا كان المساعد الذكي مُفعَّلاً، بث الردود الفورية من Claude 3 Haiku.

قاعدة البيانات تخزّن المستخدمين، الجلسات، الرسائل، والبيانات التحليلية للذكاء الاصطناعي. الجلسات تُنشَأ بأكواد دعوة للاتصال المباشر، أو يمكن للمستخدمين الدخول لنظام المطابقة الذي يُزاوج تلقائياً بين مستخدم أصم ومستخدم أبكم.

نموذج الأمان يستخدم JWT مع تشفير bcrypt للكلمات السرية. اتصالات WebSocket تتطلب تذكرة JWT قصيرة العمر تُصدَر لكل جلسة. تحديد المعدل عبر SlowAPI يمنع إساءة الاستخدام. جميع الأسرار متغيرات بيئة، ورؤوس الأمان مُضافة لجميع الاستجابات."

### الشرح التقني (للجنة التقييم)
"يعتمد النظام على فصل واضح للمسؤوليات عبر ثلاث طبقات. طبقة العرض: Next.js 14 مع App Router، TypeScript، Tailwind CSS، Framer Motion. تُدار الحالة بـ Zustand، مخزن خفيف الوزن. التواصل الفوري عبر فئة `SessionWebSocket` مخصصة مع إعادة اتصال أُسّي التراجع وكيب-الايف ping/pong.

طبقة التطبيق: FastAPI مع Python 3.11 async. معالج جلسة WebSocket (`ws_session.py`) يحتفظ بحالة لكل جلسة: مخزن صوتي (bytearray)، مدير السياق (ring buffer لسجل المحادثة)، ومدير الغرفة (سجل WebSocket في الذاكرة). طبقة خدمات AI تستخدم نمط الاستراتيجية — STTService وTTSService وOpenRouterClient قابلة للتبديل عبر التكوين.

طبقة البيانات: SQLAlchemy 2.0 async مع aiosqlite (التطوير) أو asyncpg (الإنتاج). المخطط مُطبَّع: Users → Sessions (M:N عبر session_participants) → Messages (1:N) → MessageAiMetadata (1:1). Alembic يُدير ترقيات المخطط.

المخاوف المشتركة: structlog للسجلات JSON المنظمة مع معرّفات الارتباط، SlowAPI لتحديد المعدل على أساس IP، MetricsCollector مخصص لتتبع تأخير p95، وRedux pub/sub اختياري للتوسع الأفقي لـ WebSocket."

---

## ١٨. شرح الملفات المهمة

### الملفات الخلفية الحيوية

| الملف | الغرض | الوظائف الرئيسية |
|---|---|---|
| [backend/app/main.py](backend/app/main.py) | نقطة دخول FastAPI | `lifespan()`، CORS، rate-limit، error handlers |
| [backend/app/core/config.py](backend/app/core/config.py) | الإعدادات المركزية من `.env` | فئة `Settings`، `get_settings()` |
| [backend/app/core/security.py](backend/app/core/security.py) | تشفير المصادقة | `hash_password()`، `create_access_token()`، `create_ws_ticket()` |
| [backend/app/api/v1/endpoints/ws_session.py](backend/app/api/v1/endpoints/ws_session.py) | محور WebSocket | `websocket_session()`، `handle_audio_end()`، `handle_user_text()` |
| [backend/app/services/stt_service.py](backend/app/services/stt_service.py) | تحويل الكلام إلى نص | `transcribe()`، `_transcribe_via_whisper()`، `_transcribe_via_chat()` |
| [backend/app/services/tts_service.py](backend/app/services/tts_service.py) | تحويل النص إلى كلام | `synthesize()`، `_synthesize_edge()`، `_synthesize_gpt_audio()` |
| [backend/app/services/openrouter_client.py](backend/app/services/openrouter_client.py) | عميل LLM | `stream_chat()`، `stream_action()`، `complete_with_model()` |
| [backend/app/services/ai_context.py](backend/app/services/ai_context.py) | مُصنِّف + مُحسِّن | `classify_text()`، `enhance_text()` |
| [backend/app/services/language_detector.py](backend/app/services/language_detector.py) | اكتشاف اللغة | `detect_language()`، `normalize_text()`، `compute_readability_score()` |
| [backend/app/models/user.py](backend/app/models/user.py) | نموذج ORM للمستخدم | فئة `User` |
| [backend/app/models/chat_session.py](backend/app/models/chat_session.py) | نماذج ORM للجلسة | `ChatSession`، `SessionParticipant` |
| [backend/app/models/message.py](backend/app/models/message.py) | نماذج ORM للرسالة | `Message`، `MessageAiMetadata` |

### الملفات الأمامية الحيوية

| الملف | الغرض | العناصر الرئيسية |
|---|---|---|
| [frontend/src/app/page.tsx](frontend/src/app/page.tsx) | الصفحة الرئيسية | يجمع مكوّنات الأقسام التسويقية |
| [frontend/src/app/lobby/page.tsx](frontend/src/app/lobby/page.tsx) | إدارة الجلسات | إنشاء/انضمام/مطابقة |
| [frontend/src/components/chat/ChatWorkspace.tsx](frontend/src/components/chat/ChatWorkspace.tsx) | منسّق المحادثة | يربط useSession وChatTimeline وControlDock |
| [frontend/src/lib/api/websocket.ts](frontend/src/lib/api/websocket.ts) | فئة عميل WebSocket | `SessionWebSocket`: اتصال، إعادة اتصال، نظام أحداث |
| [frontend/src/lib/hooks/useSession.ts](frontend/src/lib/hooks/useSession.ts) | جسر حالة WebSocket | يربط أحداث WS بتحديثات Zustand |
| [frontend/src/lib/state/sessionStore.ts](frontend/src/lib/state/sessionStore.ts) | الحالة العامة | Zustand store: رسائل، حالة، لغة، إمكانية الوصول |
| [frontend/src/components/avatar/HologramSigner3D.tsx](frontend/src/components/avatar/HologramSigner3D.tsx) | الصورة الرمزية 3D | مشهد Three.js، 8 أوضاع إشارة |
| [frontend/src/components/a11y/AccessibilityControls.tsx](frontend/src/components/a11y/AccessibilityControls.tsx) | لوحة إمكانية الوصول | تبديل التباين العالي، اختيار حجم الخط |
| [frontend/src/lib/i18n/en.ts](frontend/src/lib/i18n/en.ts) | الترجمة الإنجليزية | قاموس سلاسل UI الكامل |
| [frontend/src/lib/i18n/ar.ts](frontend/src/lib/i18n/ar.ts) | الترجمة العربية | قاموس سلاسل UI العربي الكامل |

### ملفات الإعداد

| الملف | الغرض |
|---|---|
| [docker-compose.yml](docker-compose.yml) | تنسيق الواجهة + الخادم + Redis |
| [backend/.env.example](backend/.env.example) | قالب جميع متغيرات بيئة الخادم |
| [frontend/.env.example](frontend/.env.example) | قالب متغيرات بيئة الواجهة |
| [backend/alembic.ini](backend/alembic.ini) | إعداد ترقيات Alembic |
| [frontend/vercel.json](frontend/vercel.json) | إعداد نشر Vercel |
| [backend/railway.toml](backend/railway.toml) | إعداد نشر Railway |

---

## ١٩. الخلاصة النهائية

### ماذا يفعل المشروع
HearMeAI هو منصة تواصل ذكية تعمل في الوقت الفعلي ومُصمَّمة خصيصاً للصم والبكم. تُمكّن التواصل الثنائي عبر تحويل الكلام إلى نص (للصم) والنص إلى كلام (للبكم)، مع مساعد ذكاء اصطناعي لتبسيط الرسائل وتوضيحها وترجمتها بين العربية والإنجليزية.

### التقنيات الرئيسية
- **الواجهة الأمامية**: Next.js 14، React 19، TypeScript، Tailwind CSS، Framer Motion، Three.js، Zustand
- **الخادم الخلفي**: FastAPI (Python 3.11)، SQLAlchemy، Alembic، bcrypt، python-jose
- **الذكاء الاصطناعي**: OpenAI Whisper (STT)، Microsoft Edge TTS، Anthropic Claude 3 Haiku (LLM)
- **قاعدة البيانات**: SQLite (التطوير) / PostgreSQL (الإنتاج)
- **البنية التحتية**: Docker، Vercel، Railway، Redis اختياري

### الميزات الرئيسية
1. تحويل الكلام إلى نص فوري مع ترجمة حية
2. تحويل النص إلى كلام بأصوات عربية وإنجليزية طبيعية
3. تبسيط الرسائل وتوضيحها وترجمتها بالذكاء الاصطناعي
4. جلسات محادثة متعددة المستخدمين مع أكواد دعوة
5. نظام مطابقة تلقائي بين الصم والبكم
6. صورة رمزية هولوغرامية ثلاثية الأبعاد للغة الإشارة
7. دعم ثنائي اللغة كامل (عربي + إنجليزي مع RTL)
8. ميزات إمكانية وصول WCAG 2.1 (تباين عالٍ، خط قابل للتكبير، تنقل بلوحة المفاتيح)
9. مصادقة مستخدمين آمنة بـ JWT
10. جاهز للإنتاج: Docker، rate limiting، سجلات منظمة، قياسات أداء

### مساهمة الذكاء الاصطناعي
طبقة الذكاء الاصطناعي هي العامل المُميِّز: Whisper يوفر دقة نسخ متعدد اللغات، وEdge TTS يوفر أصواتاً عربية وإنجليزية طبيعية مجاناً، وClaude 3 Haiku يُتيح معالجة لغوية ذكية تتجاوز مجرد الترجمة. هذه المجموعة تُحوّل النص من مجرد "قابل للقراءة" إلى "قابل للوصول" — مبسَّط وواضح ومناسب سياقياً للتواصل بين الأسوياء وذوي الإعاقة السمعية والصوتية.

### لماذا المشروع ذو قيمة
- يُعالج فجوة حقيقية في إمكانية الوصول تؤثر على 430+ مليون شخص عالمياً
- ثنائي اللغة بالكامل (عربي + إنجليزي) — العربية غير مخدومة جيداً في تقنيات المساعدة
- تطبيق متكامل: ليس نموذجاً أولياً بل نظام جاهز للإنتاج
- يُظهر تكامل APIs متعددة للذكاء الاصطناعي في تجربة مستخدم متسقة وقابلة للوصول
- هندسة قوية: Python async، TypeScript، أمان صحيح، مراقبة

### أبرز النقاط للذكر في النقاش
1. معمارية AI متعدد المزوّدين (STT/TTS/LLM قابل للتكوين مع سلاسل الاحتياط)
2. بث WebSocket الفوري لاستجابات AI الفورية والترجمات
3. دعم اللغة العربية — نص ثنائي الاتجاه، تخطيط RTL، أصوات عربية أصيلة
4. الصورة الرمزية ثلاثية الأبعاد بـ Three.js مع حركة الإيماءات الإجرائية
5. امتثال WCAG 2.1 لإمكانية الوصول (معظم مشاريع AI تتجاهل هذا)
6. بنية تحتية جاهزة للإنتاج: Docker، تحديد معدل الطلبات، JWT، سجلات منظمة
7. نظام المطابقة لتوصيل المستخدمين الصم والبكم تلقائياً

---

*التوثيق مُعَدٌّ لـ HearMeAI v1.0.0 — أبريل 2026*
