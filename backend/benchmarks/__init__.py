"""Reproducible model-comparison benchmark harness for HearMeAI.

This package computes the quantitative metrics used in the AI Model Comparison
report (WER/CER, F1, accuracy, latency percentiles, cost, and LLM-judge scores)
from real datasets and live model calls, so the methodology is auditable.
"""
